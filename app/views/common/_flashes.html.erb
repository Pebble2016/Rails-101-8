<% id flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
    <div class="alert alert-dismissable aler-<%= flash_class(key) %>">
      <button class="close" data-dismiss="alert">x</button>
    </div>
  <% end %>
<% end %>
